<h4>Additional information</h4>
<%# if !@model.comments.nil? && !@model.comments.empty? %>
    <div class="fieldset">
        <%= f.text_area :comments, :value => @model.comments.first, :id => "comments_description", data: {"progress" => "documentation"} %>

		  <%= render partial: '/shared/tooltip_base', locals: {tip_type: "documentation", tip_title: "Comment", tip_description: "Here you can record a generic comment or add notes for future reference." } %>

    </div>
<%# end %>